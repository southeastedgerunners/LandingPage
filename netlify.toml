[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"

# SPA fallback — all non-asset routes serve index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
